{
  "name": "tsol",
  "version": "1.0.6",
  "description": "A transpiler for Solidity to make assembly manageable",
  "repository": "github:merklex/tsol",
  "bin": "./run",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "merkleX",
  "private": false,
  "dependencies": {
    "@solidity-parser/parser": "^0.8.2",
    "js-sha3": "^0.8.0"
  }
}
